function areYouOldEnough(age) {
    if (age < 18) {
        console.log("You are not old enough. Age: " + age);
        return false;
    } else {
        alert("You are old enough! Age: " + age);
        return true;
    }
}

console.log( areYouOldEnough(16) );